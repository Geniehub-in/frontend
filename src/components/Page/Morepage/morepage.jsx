import React from "react";

export const morepage = () => {
  return (
    <div>
      add page component
      {/* dont forget to make folder and inside the folder component and css file, and make separate folder for each component
       */}
    </div>
  );
};
